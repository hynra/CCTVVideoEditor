<Page
    x:Class="CCTVVideoEditor.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CCTVVideoEditor.Views"
    xmlns:controls="using:CCTVVideoEditor.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Top Control Bar -->
        <CommandBar Grid.Row="0" Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}">
            <AppBarButton Icon="Folder" Label="Open Folder" Click="OpenFolderButton_Click" />
            <AppBarSeparator />
            <AppBarButton Icon="Save" Label="Export Video" Click="ExportButton_Click" IsEnabled="False" x:Name="ExportButton" />

            <CommandBar.Content>
                <TextBlock Text="CCTV Video Editor" Margin="12,14" Style="{StaticResource TitleTextBlockStyle}"/>
            </CommandBar.Content>
        </CommandBar>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Video Player -->
            <controls:VideoPlayerControl x:Name="VideoPlayer" 
                                       Grid.Column="0" 
                                       Margin="10,10,10,10"
                                       HorizontalAlignment="Stretch"
                                       VerticalAlignment="Stretch"/>

            <!-- Empty State - Shown when no videos are loaded -->
            <StackPanel x:Name="EmptyStatePanel" 
                      Grid.Column="0"
                      VerticalAlignment="Center" 
                      HorizontalAlignment="Center"
                      Visibility="Visible">
                <FontIcon Glyph="&#xE714;" 
                        FontSize="72" 
                        HorizontalAlignment="Center" 
                        Margin="0,0,0,20"/>
                <TextBlock Text="No Video Loaded" 
                         Style="{StaticResource SubtitleTextBlockStyle}" 
                         HorizontalAlignment="Center"
                         Margin="0,0,0,8"/>
                <TextBlock Text="Click 'Open Folder' to select a folder containing CCTV videos." 
                         Style="{StaticResource BodyTextBlockStyle}" 
                         HorizontalAlignment="Center"
                         TextWrapping="WrapWholeWords"
                         MaxWidth="400"/>
            </StackPanel>
        </Grid>

        <!-- Footer Status Bar -->
        <Grid Grid.Row="2" Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="StatusTextBlock" 
                     Grid.Column="0"
                     Text="Ready" 
                     Margin="12,8" 
                     VerticalAlignment="Center" />

            <!-- Placeholder for timeline (Generation 2) -->
            <Border Grid.Column="1" 
                  Background="{ThemeResource SystemControlBackgroundBaseLowBrush}" 
                  Margin="4"
                  Padding="8,4"
                  CornerRadius="4">
                <TextBlock Text="Timeline will appear here in Generation 2" 
                         HorizontalAlignment="Center"
                         FontStyle="Italic"/>
            </Border>

            <TextBlock x:Name="VideoInfoTextBlock" 
                     Grid.Column="2"
                     Text="No video loaded" 
                     Margin="12,8" 
                     VerticalAlignment="Center" />
        </Grid>
    </Grid>
</Page>